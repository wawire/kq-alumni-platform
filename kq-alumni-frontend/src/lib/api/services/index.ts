/**
 * API Services Barrel Export
 *
 * Centralized export for all API service hooks.
 *
 * @example
 * ```tsx
 * import { useSubmitRegistration, useVerifyEmail } from '@/lib/api/services';
 * ```
 */

export * from './registrationService';
